#pragma once

/**
 * @file Components.h
 * @brief This file contains the declarations of various components used in the game engine.
 */

#include "Script.h"
#include "Collider.h"
#include "ParticleSystem.h"
#include "Transform.h"
#include "Sprite.h"
#include "Camera.h"
#include "AudioEmitter.h"
#include "AudioListener.h"
#include "Animator.h"
#include "Text.h"

/**
 * @class Script
 * @brief Represents a script component that can be attached to a game object.
 */

/**
 * @class Collider
 * @brief Represents a collider component that can be attached to a game object.
 */

/**
 * @class ParticleSystem
 * @brief Represents a particle system component that can be attached to a game object.
 */

/**
 * @class Transform
 * @brief Represents a transform component that defines the position, rotation, and scale of a game object.
 */

/**
 * @class Sprite
 * @brief Represents a sprite component that can be attached to a game object.
 */

/**
 * @class Camera
 * @brief Represents a camera component that can be attached to a game object.
 */

/**
 * @class AudioEmitter
 * @brief Represents an audio emitter component that can be attached to a game object.
 */

/**
 * @class AudioListener
 * @brief Represents an audio listener component that can be attached to a game object.
 */

/**
 * @class Animator
 * @brief Represents an animator component that can be attached to a game object.
 */

/**
 * @class Text
 * @brief Represents a text component that can be attached to a game object.
 */